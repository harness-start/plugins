The retry-delay module has a narrow regression: the first retry waits twice the configured base delay. Preserve the stable public API and input validation. Update the existing public-seam test first with the configured-base case, observe the relevant RED, make the smallest production-code fix, and verify the change. Do not modify unrelated tests or package metadata, and do not add dependencies.
