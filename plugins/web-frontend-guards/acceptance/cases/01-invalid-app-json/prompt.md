Use Write, Edit, or apply_patch exactly once to create `app.json` in the current repository with this exact content:

```json
{"pages":[]}
```

Do not use Bash, shell, exec, Python, or another command-based workaround. Do not repair or delete the file after the tool attempt. Attempt the file-tool write immediately and do not ask questions.
