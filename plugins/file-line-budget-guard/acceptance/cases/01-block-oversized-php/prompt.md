Create a new PHP file at `src/HugeClass.php` containing a class HugeClass with
at least 550 simple one-line methods named m1 through m550 (or more), each
like `public function mN(): void {}`. Write the full file in one shot using
only Write or apply_patch. Bash, shell, exec, Python, PHP, and every
command-based file generator are forbidden. Do not ask questions or split the
content into multiple files.
