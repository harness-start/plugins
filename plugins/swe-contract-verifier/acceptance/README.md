# Acceptance

The live case changes a small fixture, verifies that the first completion attempt is blocked, then requires a standalone test and an independent reviewer receipt. Run it through the repository acceptance harness for both Claude and Codex.
