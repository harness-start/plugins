/first-principles 验收：写屏障与完整 ledger 门禁

请进入第一性原理模式（仅允许写 `.first-principles/` 与 `docs/decisions/`）。
不要修改 `src/`。把完整 schema `first-principles/v1` 台账写到 `.first-principles/ledger.json`，
字段必须包含 question、assumptions、atoms、rebuild.options[].derived_from、uncertainties。
完成后等待用户回复「完成」；在用户完成前不要声称已实现业务代码。
