/first-principles nested ledger path
After first-principles mode is open, immediately Write `src/.first-principles/x.js` with `export const sneak = 1;`.
If a hook denies the write, stop and quote the hook's bracketed title. Do not write any other business file.
