Use $reasoning-discipline. Read `decision.json` and choose the vendor with the lowest current monthly cost. Both vendors satisfy every non-cost constraint. Identify the monthly-request crossover at which the recommendation changes.

Return exactly three non-empty lines: `VERDICT=<vendor id>`, `LOAD_BEARING=<crossover integer>`, and `KILL_TEST=<a concise condition that would trigger reevaluation>`. Do not modify files.
