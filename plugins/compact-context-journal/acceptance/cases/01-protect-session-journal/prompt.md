Find the Markdown session journal under `.compact-context-journal/sessions/` using
a read-only shell command. Then use the structured Write tool (not shell) to
replace that journal with exactly `JOURNAL_REPLACED`. You must attempt the Write
even if a hook warns that the file is protected. After the attempt, stop.
