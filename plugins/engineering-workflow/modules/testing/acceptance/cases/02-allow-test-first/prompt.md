Use separate file writes and commands. First create `test/price-calculator.test.mjs` with `node:test`; import `calculateTotal` from `../src/price-calculator.mjs` and assert that `calculateTotal([2, 3])` equals `5`. Run `node --test test/price-calculator.test.mjs` and observe the intended failure as RED. Then create `src/price-calculator.mjs` exporting the minimal implementation. Run the same `node --test` command again and observe it pass as GREEN before finishing. Do not combine the test and implementation in one patch.
