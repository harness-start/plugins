Use separate file writes and commands. First create `test/billing/order-service.test.mjs` with `node:test`; import `createOrder` from `../../src/billing/order-service.mjs` and assert that it returns an object with the supplied id. Run `node --test test/billing/order-service.test.mjs` and observe the intended failure as RED. Then attempt the wrong same-named module `src/shipping/order-service.mjs`; observe that it is blocked. Create the correct `src/billing/order-service.mjs`, run the same test again, and observe it pass as GREEN. Do not combine test and implementation files in one patch.
