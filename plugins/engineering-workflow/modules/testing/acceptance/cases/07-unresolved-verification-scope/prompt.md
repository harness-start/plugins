`rate(-2)` in `src/rate.mjs` should return `0` instead of `-2`. Update the existing rate test first, observe RED, edit the implementation, and observe a targeted GREEN. Then run the full `node --test` suite. If another test fails, leave that unrelated file alone, rerun only the targeted rate test, and finish with one short factual status line. If a runtime guard blocks completion, follow its recovery instructions. Do not inspect plugin or hook files.
