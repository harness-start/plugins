Use $debug-workflow to diagnose and fix the failing test. Use `node --test test/math.test.mjs` as the exact reproduction and run it verbatim without pipes, redirections, an echo suffix, or another wrapper. Open the ledger with the plugin CLI writer, do not rewrite a markdown JSON snapshot, do not copy receipt ids into a file, remove any debug markers, close through the CLI after hook-backed verification, and reference the ledger path or id.
