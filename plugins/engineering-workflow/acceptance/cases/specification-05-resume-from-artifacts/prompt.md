Use `$sdd` to resume the existing `.specs/001-greeting/` change. Treat TASK-001 as already complete because its declared file and test exist and pass. Do not rewrite spec.md, plan.md, tasks.md, `src/first.mjs`, or `test/first.test.mjs`. Complete only TASK-002 by creating its two declared files, run both task Verify commands in the parent, and report the observed results. This remaining task is bounded; do not dispatch a subagent.
