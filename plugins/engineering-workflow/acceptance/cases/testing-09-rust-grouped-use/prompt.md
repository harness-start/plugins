This workspace contains a small Rust crate with one formatting function and one integration test. Update `tests/formatting.rs`: replace its import with a multiline nested grouped import for both `obf_fmt` and `obf_error`, then assert that `obf_error()` returns `"error"`. Update `src/formatting.rs` with the minimal `obf_error` implementation. Do not inspect plugin or hook files.
