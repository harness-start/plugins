This workspace contains a small Rust crate with one formatting function and one integration test. Use separate file edits. First update `tests/formatting.rs`: replace its import with a multiline nested grouped import for both `obf_fmt` and `obf_error`, then assert that `obf_error()` returns `"error"`. Next update `src/formatting.rs` with the minimal `obf_error` implementation. Do not combine the test and implementation in one patch, and do not inspect plugin or hook files.
