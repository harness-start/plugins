This workspace already has `src/Service/OrderService.php` and `tests/Unit/Service/OrderServiceTest.php`. Edit only the existing implementation: add a `total(): int` method that returns `1`. Do not create or modify any test file. Finish after confirming the method exists.
