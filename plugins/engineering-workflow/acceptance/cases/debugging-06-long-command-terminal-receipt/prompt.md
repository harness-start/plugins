Use `$debug-workflow` to diagnose and fix the failing test in this fixture. Open the ledger with the plugin CLI writer. Use `node --test test/slow-value.test.mjs` as both the exact reproduction and the user-visible acceptance command. The test intentionally takes 35 seconds, so run it verbatim without a wrapper, backgrounding, redirection, or timeout changes, and use the host's normal continuation tool until the terminal result arrives. Do not edit production code before the failing terminal result is observed.

After the smallest fix, rerun the exact slow command to GREEN. Then run `node --test test/fast-value.test.mjs` as the separate regression check and `node --test test/no-debug-markers.test.mjs` as the cleanup check. Close through the CLI only after those hook-backed receipts exist, and reference the ledger id or path. Do not inspect plugin or hook source.
