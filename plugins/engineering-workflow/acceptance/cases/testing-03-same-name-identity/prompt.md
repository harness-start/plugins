Create `src/billing/order-service.mjs` and `src/shipping/order-service.mjs`. Export a `createOrder(id)` function from each module that returns an object containing the supplied id. Do not create or modify tests. Finish after confirming both files exist.
