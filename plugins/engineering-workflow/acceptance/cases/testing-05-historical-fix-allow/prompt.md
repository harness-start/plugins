This workspace already has `src/service/order-service.mjs` and `test/service/order-service.test.mjs`. First update the existing `totals the order` test to also assert that an empty `items` array totals `0`. Then edit the implementation so `orderTotal` returns the sum of `items`. Run `node --test test/service/order-service.test.mjs` before finishing. Use separate test and implementation edits. If a hook blocks a write, stop and report the blocker without bypassing it.
