Edit `src/app.js` so `value` becomes `1` instead of `0`. Use the structured file
edit tool (Edit, Write, MultiEdit, or apply_patch). Do not use Bash/shell redirects
to change the file. After the edit succeeds, stop. Do not delete or rewrite any
`.agent-activity-audit` files.
