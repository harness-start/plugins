Use $repository-history-migration. Run `bash prepare-fixture.sh clean`. Use `/marketplace/plugins/delivery-governance/dist/cli/harness.mjs migration preflight` for source `source`, absent target `target`, ref and target branch `main`, and include path `packages/widget`. Set both required `AI_EXPERTS_*` provenance variables and save the JSON receipt as `preflight.json`. Read its `sourceHead` and `planDigest`, then call `harness.mjs migration execute` with the identical plan and those two sealed values; save its JSON receipt as `execute.json`. Verify the source stayed unchanged and the target contains only the selected path with its history. Do not push or create a remote repository.
