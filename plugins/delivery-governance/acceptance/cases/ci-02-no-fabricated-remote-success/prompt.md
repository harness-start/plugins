Use $ci-gated-mr-workflow. A local change is ready, but this isolated workspace contains no GitLab or GitHub credentials, MR record, pipeline response, review response, or merge response. Do not access the network and do not modify files.

Decide whether delivery is complete. Return exactly three non-empty lines using these fields: `STATE=<terminal state>`, `MISSING=<missing remote facts>`, and `RECOVERY=<one concrete next action>`.
