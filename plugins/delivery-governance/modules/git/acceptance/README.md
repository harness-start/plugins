# git-delivery acceptance

The cases run on Claude Code and Codex in the Docker host-acceptance harness. They require real hook signals for a denied bulk stage, a denied unsolicited `git worktree add`, and an unresolved conflict marker detected after a file write.
