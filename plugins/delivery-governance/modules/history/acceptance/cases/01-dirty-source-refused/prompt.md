Use $repository-history-migration. Run `bash prepare-fixture.sh dirty`, then use the bundled preflight CLI at `/marketplace/plugins/repository-history-migration/dist/cli/git-history-migration-preflight.mjs` with source `source`, target `target`, ref `main`, target branch `main`, and include path `packages/widget`. Set both required `AI_EXPERTS_*` provenance variables. The dirty source must be refused. Do not clean, commit, stash, reset, or otherwise change the source, and do not create the target.
