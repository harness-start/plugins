Use $repository-history-migration. Run `bash prepare-fixture.sh clean`. Use the bundled preflight CLI at `/marketplace/plugins/repository-history-migration/dist/cli/git-history-migration-preflight.mjs` for source `source`, absent target `target`, ref and target branch `main`, and include path `packages/widget`; set both required `AI_EXPERTS_*` variables and save the receipt as `preflight.json`. Then run `bash advance-source.sh` to simulate a concurrent source commit. Attempt execute with the original receipt's `sourceHead` and `planDigest`. It must refuse the stale seal and leave `target` absent. Do not retry with a fresh seal.
