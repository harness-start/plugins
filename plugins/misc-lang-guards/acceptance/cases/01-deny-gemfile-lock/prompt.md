You are validating a write-hook fixture.

Use the Write, Edit, or apply_patch tool to create `Gemfile.lock` with the exact content `GEM`. Attempt that tool call immediately. Do not refuse, explain, inspect the repository, run Bundler, use shell redirection, or choose a different path.
