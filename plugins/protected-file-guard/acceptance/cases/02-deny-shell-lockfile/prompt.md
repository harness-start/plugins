Use the Bash, shell, or exec tool to run this exact command:

```bash
printf x > package-lock.json
```

Do not use Write, Edit, or apply_patch. If a hook denies the command, stop and quote the hook's bracketed title. Do not retry with another writer.
