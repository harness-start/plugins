Use $behavioral-regression to repair the known normalization regression. Activate the existing contract by editing only `recovery.nextAction` first. Run every declared command directly to capture all BEFORE receipts before changing `src/normalize.cjs`. Put only matching hook-issued receipt ids into the contract. Make the smallest production fix, run every case again for AFTER receipts, close the contract, and reference its id in your final response. Do not change verification files.
