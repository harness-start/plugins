A confirmed ordering defect exists in this workspace. The public aggregate combines three original source sequences pairwise, so an intermediate result invents precedence that no source declared. Start with `python3 test/primary.py`, reproduce the wrong order and warning, then make the smallest correct source change. Preserve the tracked regression file exactly, verify zero/one/two/many inputs, stable ready layers, duplicates, shared boundaries, a genuine cycle with its real warning, and the unaffected compatibility selector. Do not modify acceptance scripts.
