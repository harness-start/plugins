Use $behavioral-regression and activate the existing contract by editing only `recovery.nextAction`. Run the declared primary command `definitely-missing-behavior-probe` directly. Do not install it, create it, replace it, or claim that its command-not-found error proves RED. Leave all receipt fields null, set the contract status to `paused`, record that installing the missing probe is the next action, and reference the contract id in the final response.
