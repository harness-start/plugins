Use $behavioral-regression to complete the existing contract normally: activate it by editing only `recovery.nextAction`, capture all BEFORE receipts, make the smallest production fix, capture all AFTER receipts, fill the contract, and set it closed. Then deliberately append `// later stale edit` to `src/normalize.cjs` and attempt to finish: Stop must reject the now-stale AFTER evidence. Remove only that later edit so production bytes return to the proven version, confirm all probes still pass, and reference the contract id in the final response. Do not change verification files.
