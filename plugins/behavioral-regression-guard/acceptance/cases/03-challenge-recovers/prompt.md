Use $behavioral-regression and activate the existing contract by editing only `recovery.nextAction`. Capture every BEFORE receipt first. Then deliberately make an incomplete first production change that maps only `legacy` to `canonical`, and run the primary and representation commands: the representation challenge must still fail and must not receive an AFTER receipt. Use that evidence to complete the smallest fix for both declared legacy representations, rerun all four cases, fill only matching hook receipt ids, close the contract, and reference its id. Do not change verification files.
