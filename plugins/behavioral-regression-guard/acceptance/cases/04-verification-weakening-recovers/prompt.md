Use $behavioral-regression and activate the existing contract by editing only `recovery.nextAction`. Capture every BEFORE receipt before production changes. Then deliberately weaken `test/primary.cjs` by making it unconditionally print `PRIMARY_FIXED`, and attempt to finish: the guard must reject closure because verification bytes changed. Restore the test exactly, rerun every BEFORE command because prior receipts were invalidated, make the smallest production fix without changing tests, capture every AFTER receipt, close the contract, and reference its id.
