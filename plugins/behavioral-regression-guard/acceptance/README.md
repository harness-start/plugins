# Acceptance

Every case runs in fresh Claude Code and Codex sessions. Active cases require both an observable final world state and a real Behavioral Regression Guard signal. The suite covers no-op routing, successful closure, recovery from an incomplete fix, test-asset tampering, stale post-fix evidence, an unavailable command, failure-armed production writes, automatic bootstrap for partial multi-component inputs, independently checked topological layers, source-bound variadic seams across zero, one, two, and many contributors, three-or-more composition repaired through its existing constraint-forming primitive, protected project regression tests, and relational preservation of every populated peer under asymmetric partial degeneracy. The Python ordering case additionally requires immutable v11 supersession metadata and binds a genuine-cycle diagnostic to an identity projection of the warning captured around the same seam call.
