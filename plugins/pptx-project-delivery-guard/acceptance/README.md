# PPTX guard acceptance

The suite runs each case on Claude Code and Codex. It proves ordinary prompts do not activate the guard and that a direct generated-file write is denied before the file appears.
