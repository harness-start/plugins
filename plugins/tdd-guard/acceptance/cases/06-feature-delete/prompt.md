This workspace already has `src/service/discount.mjs` and `test/service/discount.test.mjs`. Remove the discount feature. Delete the existing test file first, then delete the implementation file. Do not write a replacement test, and do not run a test command just to manufacture RED. If a hook blocks a delete, stop and report the blocker without bypassing it. Do not recreate either file.
