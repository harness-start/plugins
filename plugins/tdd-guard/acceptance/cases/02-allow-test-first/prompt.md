Use two separate file writes. First create `tests/Unit/PriceCalculatorTest.php` in namespace `Tests\Unit`; import `App\PriceCalculator` and PHPUnit's `CoversClass`, add `#[CoversClass(PriceCalculator::class)]`, and define a PHPUnit-style `test_calculates_total` method that instantiates `PriceCalculator`. After that write completes, create `src/PriceCalculator.php` in namespace `App` with a minimal `PriceCalculator` class. Do not combine the test and implementation in one patch, and do not run tests.
