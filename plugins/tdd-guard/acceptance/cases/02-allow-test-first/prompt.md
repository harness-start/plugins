Use two separate file writes. First create `tests/Unit/PriceCalculatorTest.php` with a PHPUnit-style `test_calculates_total` method that instantiates `PriceCalculator`. After that write completes, create `src/PriceCalculator.php` with a minimal `PriceCalculator` class. Do not combine the test and implementation in one patch, and do not run tests.
