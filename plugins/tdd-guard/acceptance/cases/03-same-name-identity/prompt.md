Use separate file writes. First create `tests/Unit/Billing/OrderServiceTest.php` in namespace `Tests\Unit\Billing`; import `App\Billing\OrderService` and PHPUnit's `CoversClass`, add `#[CoversClass(OrderService::class)]`, and define `test_creates_order` that instantiates `OrderService`. Then attempt to create `src/Shipping/OrderService.php` in namespace `App\Shipping` with an `OrderService` class. When the hook blocks that same-named but unrelated class, do not bypass it; instead create `src/Billing/OrderService.php` in namespace `App\Billing` with an `OrderService` class. Do not combine test and implementation files in one patch, and do not run tests.
