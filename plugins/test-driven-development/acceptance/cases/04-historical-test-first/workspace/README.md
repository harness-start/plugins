# Synthetic workspace

This workspace already contains `src/Service/OrderService.php` and `tests/Unit/Service/OrderServiceTest.php`.
Direct source edits must be blocked until those existing tests change or already-fail.
