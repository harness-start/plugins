`cap(-3)` in `src/cap.mjs` should return `0` instead of `-3`. Update the existing test first and run it to observe RED. Make a first implementation attempt that returns `1` for negative values and run the test to observe that it is still RED. Then correct the implementation to return `0`, rerun the test to GREEN, and finish with one short factual status line. Preserve positive values and do not inspect plugin or hook files.
