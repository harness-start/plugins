This workspace already has `src/service/order-service.mjs` and `test/service/order-service.test.mjs`. The existing tests already fail. Do not create or modify any test file. Run `node --test test/service/order-service.test.mjs`, observe the failure (RED), then edit only the existing implementation so `orderTotal` returns the sum of `items`. Run the same test command again and observe it pass (GREEN) before finishing. If a hook blocks a write, stop and report the blocker without bypassing it.
