Change the public authorization function in `src/access.mjs` so suspended users are always denied, including administrators and record owners, while preserving access for active administrators and active owners. Update the existing tests. This is an authentication and public-API change: after the first coherent implementation and focused tests pass, run the applicable engineering review checkpoint with exactly one read-only reviewer. Do not create unrelated files. Finish with a short factual status.
