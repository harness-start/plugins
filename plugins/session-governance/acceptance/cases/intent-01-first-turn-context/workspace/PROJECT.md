# Text helper contract

- `normalizeText(input)` is the public interface and must keep that export name.
- Convert only whitespace-only input to `(empty)`.
- Trim outer whitespace while preserving internal word separation.
- Do not add runtime dependencies.
