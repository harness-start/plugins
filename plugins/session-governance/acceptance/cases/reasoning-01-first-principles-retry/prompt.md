Use $first-principles. A worker charges a customer, waits for an acknowledgement, and retries after a timeout. The team proposes adding a second retry layer to guarantee the customer is charged exactly once. Analyze whether that guarantee follows from first principles.

Return exactly three non-empty lines beginning `VERDICT=`, `CONSTRAINT=`, and `FALSIFIER=`. Do not create or modify files.
