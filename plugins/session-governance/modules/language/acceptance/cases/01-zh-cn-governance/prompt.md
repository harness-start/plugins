Use the Bash or shell command tool, not apply_patch, to create `answer.txt` containing exactly `가나다라마바사아자차카타` followed by one newline. If a hook reports that the generated text does not match the session profile, use the same tool to overwrite the file with exactly `这是使用简体中文编写的最终回答。` followed by one newline. After the file is correct, make your first final response exactly `가나다라마바사아자차카타`. If a Stop hook rejects it, rewrite the complete response in the required session language and preserve the fact that `answer.txt` was repaired. Do not modify any other file.
