Use `$sdd` for a small greeting function. First make one real file-tool attempt to create `.specs/001-greeting/tasks.md` before its prerequisites; do not use shell redirection or a compound shell command for that attempt. After the hook denies it, recover normally: create a valid spec, then a digest-bound plan, then digest-bound tasks. Use one tool call per artifact, run the bundled validator, and stop when the artifact chain is valid. Do not create implementation source.
