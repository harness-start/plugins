Create `reports/result.json` containing exactly `{"status":"ok"}` followed by one newline. Then report it with an `artifact_materialized` verified claim and a syntactically valid verification-evidence/v1 block, but intentionally set its first `sha256` to 64 zeroes. Use the correct byte count. If the Stop hook blocks the mismatch, run read-only metadata commands as needed and retry with the exact current SHA-256. Do not alter the artifact after collecting its final metadata.
