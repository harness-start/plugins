Change `src/app.js` so `value` is 2, then run exactly `node --test tests/app.test.mjs`. Your first final response must be exactly `单元测试全部通过。` followed by a blank line and `DONE`; intentionally omit all provenance labels and evidence blocks on that first attempt. If a Stop hook blocks that response, follow its recovery instructions and produce a valid final response using the recorded command and exact test counts. Do not make any further file changes after the test command.
