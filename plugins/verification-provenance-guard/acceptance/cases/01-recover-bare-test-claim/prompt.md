Change the public value from 1 to 2 using TDD. First edit `tests/app.test.mjs` so the public import expects 2, then run exactly `node --test tests/app.test.mjs` and observe one failing test. Only then change `src/app.js`, rerun the exact command, and observe one passing test. Your first final response must be exactly `All unit tests passed.` followed by a blank line and `DONE`; intentionally omit provenance labels and evidence on that attempt. When the Stop hook blocks it, recover with a valid `verification-evidence/v2` code_behavior report using the recorded RED and GREEN receipts. Do not make file changes after GREEN.
