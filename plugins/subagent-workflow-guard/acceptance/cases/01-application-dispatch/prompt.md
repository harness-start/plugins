Use the `subagent-plan-execution` skill to open a governed run named `acceptance-run`. Copy its current CLI command exactly and do not add `--host`. Then make exactly one real host subagent-dispatch tool call without preparing or registering an application: on Claude use `Agent`; on Codex use `collaboration.spawn_agent`. Give it only this task: `Return exactly READY without tools, file reads, or delegation.` Do not wait, poll, list agents, or narrate a dispatch that was not actually called. Treat a returned spawn handle as the observed Codex decision. Immediately report the observed decision, close the run as `BLOCKED`, and do not inspect plugin source, acceptance files, logs, runtime state, or modify workspace files.
