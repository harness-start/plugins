# engineering-quality acceptance

All cases run on Claude Code and Codex in the Docker host-acceptance harness. `01-repair-javascript-syntax` verifies the static-code check and repaired JavaScript, `02-block-oversized-php` verifies the file-budget signal, and `03-fix-heading-jump` verifies the Markdown hierarchy signal and repaired document.
