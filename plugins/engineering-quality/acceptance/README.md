# engineering-quality acceptance

All cases run on Claude Code and Codex in the Docker host-acceptance harness. `02-block-oversized-php` verifies the language-neutral file-budget signal, and `03-fix-heading-jump` verifies the Markdown hierarchy signal and repaired document. Language-specific syntax and dependency-file checks live in their owning domain plugins.
