Use Write or Edit to replace `src/app.js` with complete content exactly `export const answer = ;`. This first write is required. When the post-write hook rejects the syntax, repair the same file with complete content exactly `export const answer = 42;`. Bash, shell, exec, Python, Node, and command-based file writers are forbidden. Do not modify any other file. In the final response, report the hook's bracketed title exactly as observed.
