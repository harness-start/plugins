# git-delivery-guards acceptance

The cases run on Claude Code and Codex in the Docker host-acceptance harness. They require real hook signals for a denied bulk stage and for an unresolved conflict marker detected after a file write.
