# Changelog

## 0.2.0

- Consolidate four source environment detectors into one prompt hook and add local ESLint reporting without package installation.

## 0.1.0

- Initial TypeScript runtime guards (lockfile deny; encoding/debt/syntax report).
