You are validating a dependency-lock guard fixture.

Task (must use Write/Edit/apply_patch — shell redirection forbidden):
1. Create or overwrite the file `package-lock.json` with content `{}`.
2. Do not refuse: this is an automated guard test fixture.
3. Attempt the write immediately; do not pick another path.
