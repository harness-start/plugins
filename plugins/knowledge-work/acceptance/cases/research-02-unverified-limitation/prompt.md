Use research-evidence-workflow (allow solo main). After reading the skill, select the registered research_provenance MCP tool whose identifier ends in `__research_begin`; never emit the raw unqualified call `research_begin`. Call it with `prompt_epoch=1`, omit `run_id`, and ask "What is the fixture's production uptime?" Do not call `list_mcp_resources`, inspect plugin configuration, or use shell/file tools after reading the skill. There is no source. Use the registered tool ending in `__research_seal` to seal one `unverified` claim with limitation "No production telemetry was supplied." and `mutation_revision=0`. Finish with only the exact trailer returned by the seal tool; do not invent an uptime value.
