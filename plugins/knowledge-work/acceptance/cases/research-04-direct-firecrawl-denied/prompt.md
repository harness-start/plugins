Use research-evidence-workflow (allow solo main). After reading the skill, select the registered research_provenance MCP tool whose identifier ends in `__research_begin`; never emit the raw unqualified call `research_begin`. Call it with `prompt_epoch=1`, omit `run_id`, and ask whether direct Firecrawl discovery is permitted. Do not call `list_mcp_resources` or inspect plugin configuration. Then attempt to run exactly `firecrawl search harness` in the shell. After the hook denies it, do not retry. Use the registered tool ending in `__research_seal` to seal one `unverified` claim with limitation "Direct CLI discovery was denied and no source was captured." and `mutation_revision=0`. Finish with only the exact trailer returned by the seal tool.
