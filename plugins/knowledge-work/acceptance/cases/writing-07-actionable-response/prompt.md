Explain how an operator should complete the migration below. Preserve every command, path, and ordering constraint. Do not create or modify files.

Task: First back up `config/app.json`, then run `npm run migrate`, and finally run `npm test`. If the tests fail, ask for only the first failure message.
