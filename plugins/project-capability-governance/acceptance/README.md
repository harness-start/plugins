# project-capability-governance acceptance

The dual-host cases verify that a new project proposal produces a human-only, non-blocking Stop notice and that an ordinary task with no proposal remains unaffected. Unit tests cover recorder binding and safe lifecycle mutations because those seams require deterministic hook-event identities.
