This is an explicit request to standardize a durable project capability. Do not
spawn a subagent. Create exactly one valid pending SOP proposal at
`.project-capabilities/inbox/pending/pc-repeatable-release-check.md` yourself.

The SOP is: before each release, run the same unit verification command and
record its result. This request is explicit standardization. Include two future
reuse scenarios, measurable acceptance, and a counterexample. Then report
whether that exact proposal exists.
