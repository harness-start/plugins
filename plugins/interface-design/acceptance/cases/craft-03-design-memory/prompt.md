Use interface-craft to materially redesign this small product overview without adding dependencies or changing its copy. Establish a reusable visual direction, semantic tokens, component/state rules, responsive behavior, and motion guidance. Update the existing root DESIGN.md with the plugin's managed design-memory block while preserving all project-authored text outside it. Keep implementation in the existing HTML and CSS files and record only evidence you can distinguish as facts, inferences, or falsifiable assumptions.
