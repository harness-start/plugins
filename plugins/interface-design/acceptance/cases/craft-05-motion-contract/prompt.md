Use interface-craft to finish the FAQ disclosure in this workspace without dependencies. Keep it a semantic button-controlled disclosure, add semantic motion duration/easing tokens, enumerate only the properties that should animate, make close and immediate reopen converge on the latest state without timeout cleanup, and provide a meaningful reduced-motion path. Preserve keyboard focus and synchronize aria-expanded with the visible state.
