Improve the account overview in this workspace without changing its copy or adding dependencies. Preserve the existing navy and teal brand colors, keep reusable brand and surface values in shared CSS custom properties, make the information hierarchy clear, give the primary action accessible hover, active, and keyboard-focus states, and make the page genuinely reflow at both desktop and narrow mobile widths without horizontal overflow. Add one restrained transition using semantic motion duration/easing custom properties, enumerate the transitioned properties, and include a meaningful `prefers-reduced-motion` fallback. Keep the implementation in the existing HTML and CSS files.
