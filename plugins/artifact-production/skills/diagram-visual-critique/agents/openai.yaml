interface:
  display_name: "Diagram Visual Critique"
  short_description: "Read-only visual and semantic critique for an existing diagram"
  default_prompt: "Use $diagram-visual-critique to: Read-only visual and semantic critique for an existing diagram"
