# Logo guard acceptance

Both hosts exercise ordinary bypass, protected master denial, incomplete release closure, wrapper-substring spoof denial, and existing-plan downgrade denial. Claude additionally proves that a review subagent receives a trusted agent-scoped principal and can return without a `SubagentStop` closure block.
