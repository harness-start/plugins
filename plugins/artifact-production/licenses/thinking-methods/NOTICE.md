# Thinking-method provenance

`artifact-creative-direction` is original synthesis informed by audits of `danium/lateral-thinking` at `bde713d`, `kcchien/model-thinking` at `c4027be`, and `mattnowdev/thinking-partner` at `ce95b62`. This plugin remains self-contained; the Skill is advisory and introduces no external runtime dependency.
