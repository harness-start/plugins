# Synthetic clean control

Private plan:

- `audience.primary`: prospective investors and enterprise buyers
- `audience.addressing`: `implicit`
- intended action: request a technical pilot

Rendered montage:

1. The cover title is “Model gateway”; the subtitle names the operational benefit and does not mention who the presentation is for. The remaining title chain is “Fragmented trials”, “Operating path”, “Supported clients”, and “Pilot decision”; its syntax varies and every finding points to visible evidence.
2. Slide 2 uses a two-column before/after comparison, slide 3 uses a left-to-right native flow with a visible return edge, slide 4 uses a full-width product screenshot plus a left-aligned bullet list, and slide 5 uses a single metric chart with a qualification.
3. Body and list text is left/top aligned. OOXML evidence records explicit line spacing and paragraph-after spacing with no autofit; adjacent baselines align.
4. The native flow endpoints touch their declared nodes, forward arrows follow the declared direction, and the return arrow closes the loop. Titles are short, no text is clipped, and contrast passes.
