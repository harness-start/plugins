Use research-evidence-workflow (allow solo main). After reading the skill, select the registered research_provenance MCP tool whose identifier ends in `__research_begin`; never emit the raw unqualified call `research_begin`. Call it with `prompt_epoch=1` and omit `run_id`; this creates the project workflow. Do not call `list_mcp_resources`, inspect plugin configuration, or use shell/file tools after reading the skill. Use only registered research_provenance MCP tools ending in `__source_capture`, `__source_read`, `__source_anchor`, and `__research_seal`: capture local `source.md`, read it, create an exact anchor, and seal one anchored claim with `mutation_revision=0`. Finish with only the exact trailer returned by the seal tool.
