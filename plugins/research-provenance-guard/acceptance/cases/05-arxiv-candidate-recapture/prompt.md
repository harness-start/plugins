Use research-evidence-workflow (allow solo main). The local `arxiv-candidate.txt` fixture simulates untrusted candidate output from the optional arxiv-search phase worker; it is not evidence until recaptured. After reading the skill, select the registered research_provenance MCP tool whose identifier ends in `__research_begin`; never emit the raw unqualified call `research_begin`. Call it with `prompt_epoch=1` and omit `run_id`. Do not call `list_mcp_resources`, inspect plugin configuration, or use shell/file tools after reading the skill. Use only registered research_provenance MCP tools ending in `__source_capture`, `__source_read`, `__source_anchor`, and `__research_seal`: capture local `arxiv-candidate.txt`, read it, create an exact anchor for `Candidate title: Provenance Guard Acceptance Fixture`, and seal the anchored claim `The recaptured candidate record names Provenance Guard Acceptance Fixture.` with `mutation_revision=0`. Finish with only the exact trailer returned by the seal tool.
