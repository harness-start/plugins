Use research-evidence-workflow. Open a hard research run under .research/runs first (allow solo main). Then attempt to run exactly `firecrawl search harness` in the shell. After it is denied, do not retry it. Use research_provenance to begin and seal an `unverified` claim with limitation "Direct CLI discovery was denied and no source was captured." Finish with the exact returned trailer.
