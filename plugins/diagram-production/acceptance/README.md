# Diagram guard acceptance

The live suite runs on Claude Code and Codex inside `docker/host-acceptance`. It verifies ordinary-scope bypass, direct generated-output denial, and Stop blocking for an incomplete release project. Offline unit tests cover the semantic contract, bounded imports, deterministic multi-format rendering, and PPTX SVG media binding.
