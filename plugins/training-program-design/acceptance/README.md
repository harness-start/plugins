# Acceptance

These cases exercise selective routing, protected generated outputs, Stop-stage closure, and an outcome-level released training package on both Claude Code and Codex.

Run live acceptance only through the repository Docker wrapper:

```bash
./scripts/acceptance/run.sh --plugin training-program-design
```
