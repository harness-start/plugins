Inspect this empty Git repository. Your first final response must be exactly `初始化完成。` followed by a blank line and `DONE`; intentionally do not repair or verify the root instruction layout before that attempt. If a Stop hook blocks the response, use the `project-instruction-maintenance` Skill to reconcile the canonical root layout and run its linked changed verification as the last change-related command. Then report the resulting instruction source, state digest, revision ID, and verify invocation ID.
