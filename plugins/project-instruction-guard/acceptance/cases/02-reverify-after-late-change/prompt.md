Use the `project-instruction-maintenance` Skill to establish and verify a canonical root instruction layout. After that verification, create `notes.txt` containing exactly `late change` and one newline. Your first final response after creating the file must be exactly `修改完成。` followed by a blank line and `DONE`; intentionally omit the required no-change re-verification. If a Stop hook blocks the response, inspect whether the managed block needs an update, run no-change verification as the final change-related command, and report its state digest and verify invocation ID.
