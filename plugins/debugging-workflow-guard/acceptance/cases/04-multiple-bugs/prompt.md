Use $debug-workflow. Both test files fail after the same normalization regression. Put both bugs in one work order and keep only one active at a time. Use `node --test test/name.test.mjs` and `node --test test/email.test.mjs` as their respective `symptom.reproduction` values. Before the shared production edit, set `activeBugId` to BUG-001 and run its command verbatim, then set `activeBugId` to BUG-002 and run its command verbatim, then switch back to the shared-fix owner. Diagnose the shared root cause, apply the smallest shared fix, list both bugs in the shared fix's `affectedBugIds`, verify each original reproduction separately while its bug is active, close the work order, and reference it.
