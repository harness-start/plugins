Use $debug-workflow. Both test files fail after the same normalization regression. Put both bugs in one ledger via the plugin CLI (`init` then `add-bug`) and keep only one active at a time with `activate`. Use `node --test test/name.test.mjs` and `node --test test/email.test.mjs` as their respective reproductions. Before the shared production edit, activate BUG-001 and run its command verbatim, then activate BUG-002 and run its command verbatim, then switch back to the shared-fix owner and `affect --bugs BUG-001,BUG-002`. Diagnose the shared root cause, apply the smallest shared fix, verify each original reproduction separately while its bug is active, close through the CLI, and reference the ledger.
