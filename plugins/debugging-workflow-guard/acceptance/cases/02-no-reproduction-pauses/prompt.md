Use $debug-workflow. Diagnose the reported bug that a private payment sandbox sometimes declines a card, but this fixture has no credentials, logs, endpoint, or reproduction. Do not change `src/payment.js`. Create the required work order, record the honest blocker and recovery action, set both top-level `status` and `run.state` to `paused`, ensure the `json debug-work-order/v1` fence has its closing triple-backtick line, and reference it in your response.
