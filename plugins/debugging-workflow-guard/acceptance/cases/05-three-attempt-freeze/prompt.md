Use $debug-workflow to exercise the failed-fix guard on the test in this fixture. Create one active bug whose exact reproduction is `node --test test/value.test.mjs`; run it verbatim without wrappers and observe the initial failure. Record a supported hypothesis/root cause and move the bug/fix to `fixing`/`in-progress`. Then make three separate production edits setting `value` to 1, 2, and 3, running that same exact command verbatim after each edit; all three must remain failing because the expected value is 4. Attempt a fourth production edit, stop when the hook blocks it, move the bug to architecture-review, set top-level `status` and `run.state` both to `paused` with a recovery action, and reference it. This is a guard acceptance fixture, so do not skip or combine the deliberately unsuccessful candidates.
