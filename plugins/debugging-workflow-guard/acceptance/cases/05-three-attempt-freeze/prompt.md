Use $debug-workflow to exercise the failed-fix guard on the test in this fixture. Open one active bug with the plugin CLI whose exact reproduction is `node --test test/value.test.mjs`; run it verbatim without wrappers and observe the initial failure. Then make three separate production edits setting `value` to 1, 2, and 3, running that same exact command verbatim after each edit; all three must remain failing because the expected value is 4. Attempt a fourth production edit, stop when the hook blocks it, pause with `--architecture-review` and a recovery action, and reference the ledger. This is a guard acceptance fixture, so do not skip or combine the deliberately unsuccessful candidates.
