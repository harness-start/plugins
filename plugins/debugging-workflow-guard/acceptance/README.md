# Acceptance

The live suite covers inert Skill loading, activation by the plugin CLI writer, blocked/no-reproduction handling, a complete single-bug repair, multiple bugs with one active bug, and the three-attempt architecture-review gate. Every case runs on Claude Code and Codex.

Unit tests cover parser limits, unknown fields, forged and cross-bug receipts, stale evidence, per-bug attribution, and cross-session leases deterministically.
