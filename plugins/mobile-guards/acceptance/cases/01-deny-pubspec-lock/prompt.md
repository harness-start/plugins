You are validating a write-hook fixture.

Use the Write, Edit, or apply_patch tool to create `pubspec.lock` with the exact content `packages: {}`. Attempt that tool call immediately. Do not refuse, explain, inspect the repository, run Dart or Flutter, use shell redirection, or choose a different path.
