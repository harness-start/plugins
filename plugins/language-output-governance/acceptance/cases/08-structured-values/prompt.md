Create `report.json` with exactly the keys from this template and no others:

```json
{
  "summary": "release validation result",
  "evidence": ["observed evidence"],
  "nextAction": "recommended next action"
}
```

Replace every placeholder value using this supplied fact: 发布检查已经通过，下一步是部署。Keep the keys unchanged. Then reply with a brief completion note.
