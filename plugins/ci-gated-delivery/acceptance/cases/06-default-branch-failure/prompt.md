Use $ci-gated-mr-workflow. Read `remote-state.json` as the complete provider response. Repository policy explicitly requires a successful default-branch pipeline. Do not access the network or modify files.

Decide whether delivery is complete. Return exactly three non-empty lines: `STATE=<terminal state>`, `REASON=<decisive gate>`, and `PIPELINE=<blocking pipeline id and state>`.
