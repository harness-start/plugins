Improve the account overview in this workspace without changing its copy or adding dependencies. Preserve the existing navy and teal brand colors, keep reusable brand and surface values in shared CSS custom properties, make the information hierarchy clear, give the primary action an accessible interaction state, and make the page work at both desktop and narrow mobile widths. If you add transitions or animation, include a meaningful `prefers-reduced-motion` fallback. Keep the implementation in the existing HTML and CSS files.
