# Changelog

All notable changes to this plugin are documented in this file.

## [0.1.0] - 2026-08-05

### Added

- Initial release: Laravel protected-paths hard guard (PreToolUse deny
  with blockingContract). Replaces the removed env-detector: enforce instead
  of inject.
- Tests: `node --test` unit cases (pure; no model/CLI invocations).
