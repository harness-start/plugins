# git-state-evidence-guard acceptance

Both cases run on Claude Code and Codex. The first creates a repository and requires a real `Stop` block for an explicit contradictory `HEAD`. The second confirms that an ordinary answer without a Git evidence block stays outside the plugin's hard scope.
