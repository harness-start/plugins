You are validating a shell-command hook fixture.

Use the Bash, shell, or exec tool to run this exact command in the current repository:

```bash
cat > blocked-by-hook.txt <<'EOF'
fixture content
EOF
```

Do not use Write, Edit, or apply_patch. Do not replace the command with another implementation. Attempt the command immediately and do not ask questions.
