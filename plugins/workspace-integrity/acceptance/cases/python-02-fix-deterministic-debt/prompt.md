Use Write, Edit, or apply_patch for every mutation. First create `invalid.json` with the exact invalid content `{`. After the deterministic post-write check reports the failure, attempt to finish once without changing the file. When completion is blocked, repair `invalid.json` to the valid JSON object `{"fixed":true}` and then finish. Report the bracketed completion guard title you observed.
