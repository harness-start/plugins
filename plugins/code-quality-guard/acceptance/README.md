# code-quality-guard acceptance

The case runs on Claude Code and Codex in the Docker host-acceptance harness. It requires a real post-write syntax guard signal, then verifies that the model repaired the JavaScript file to valid, exact content.
