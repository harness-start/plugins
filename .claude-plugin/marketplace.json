{
  "name": "harness-start",
  "description": "Harness Start plugins for Claude Code and Codex.",
  "owner": {
    "name": "Harness Start",
    "email": "devtools@example.com"
  },
  "plugins": [
    {
      "name": "file-line-budget-guard",
      "source": "./plugins/file-line-budget-guard",
      "description": "Ratchet-enforced file line budget guard: prevents source files from exceeding per-language line limits."
    },
    {
      "name": "php-runtime-guards",
      "source": "./plugins/php-runtime-guards",
      "description": "PHP runtime guards: composer.json policy (repositories / unicode escapes / lockfile), protected generated paths, test output truncation, php -l and composer validate syntax, encoding, net-new debt and debug statements."
    },
    {
      "name": "symfony-runtime-guards",
      "source": "./plugins/symfony-runtime-guards",
      "description": "Symfony runtime guards: protected generated paths (symfony.lock, var/cache, public/build, migrations), Doctrine entity mapping heuristics, Twig template syntax checks."
    },
    {
      "name": "process-confidence",
      "source": "./plugins/process-confidence",
      "description": "Observable delivery process: validated session begin, hook receipts, auto complete, Stop gate (hooks never auto-begin)."
    },
    {
      "name": "laravel-runtime-guards",
      "source": "./plugins/laravel-runtime-guards",
      "description": "Laravel hard guards: deny writes to generated paths (bootstrap/cache, storage/framework cache|views, storage/logs, public/build, existing database/migrations)."
    },
    {
      "name": "thinkphp-runtime-guards",
      "source": "./plugins/thinkphp-runtime-guards",
      "description": "ThinkPHP hard guards: deny writes to runtime directories (modern runtime/, legacy Application/Runtime/)."
    },
    {
      "name": "webman-runtime-guards",
      "source": "./plugins/webman-runtime-guards",
      "description": "Webman hard guards: deny writes to the runtime directory (logs, pid files, caches)."
    }
  ]
}
